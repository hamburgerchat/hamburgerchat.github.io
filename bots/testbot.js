class ImageGeneratorBot {
    constructor() {
        this.name = "ГенераторФото";
        this.greetings = ["привет", "hello", "здравствуй", "хай", "ку"];
        this.farewells = ["пока", "до свидания", "прощай", "bye"];
        this.requests = ["сгенерируй", "нарисуй", "создай", "покажи", "фото", "картинку", "изображение"];
        this.translations = {
            "кошка": "cat",
            "собака": "dog",
            "дерево": "tree",
            "город": "city",
            "лес": "forest",
            "море": "sea",
            "горы": "mountains",
            "космос": "space",
            "космонавт": "astronaut",
            "робот": "robot",
            "дракон": "dragon",
            "единорог": "unicorn",
            "закат": "sunset",
            "рассвет": "sunrise",
            "цветы": "flowers",
            "архитектура": "architecture",
            "фэнтези": "fantasy",
            "научная фантастика": "sci-fi",
            "абстракция": "abstract",
            "портрет": "portrait",
            "пейзаж": "landscape",
            "животное": "animal",
            "птица": "bird",
            "рыба": "fish",
            "насекомое": "insect",
            "еда": "food",
            "фрукты": "fruits",
            "овощи": "vegetables",
            "машина": "car",
            "самолет": "airplane",
            "корабль": "ship",
            "здание": "building",
            "дом": "house",
            "улица": "street",
            "парк": "park",
            "сад": "garden",
            "река": "river",
            "озеро": "lake",
            "океан": "ocean",
            "пляж": "beach",
            "пустыня": "desert",
            "снег": "snow",
            "дождь": "rain",
            "шторм": "storm",
            "молния": "lightning",
            "радуга": "rainbow",
            "облака": "clouds",
            "звезды": "stars",
            "планета": "planet",
            "галактика": "galaxy",
            "чёрная дыра": "black hole",
            "супергерой": "superhero",
            "фея": "fairy",
            "волшебник": "wizard",
            "рыцарь": "knight",
            "замок": "castle",
            "пещера": "cave",
            "остров": "island",
            "водопад": "waterfall",
            "вулкан": "volcano",
            "огонь": "fire",
            "вода": "water",
            "воздух": "air",
            "земля": "earth"
        };
    }

    async onStart(context) {
        await context.reply({
            text: "🎨 Привет! Я бот-генератор изображений!\n\n" +
                  "Я могу создавать картинки по твоему описанию.\n\n" +
                  "**Как использовать:**\n" +
                  "• Напиши что хочешь увидеть на картинке\n" +
                  "• Используй слова: \"сгенерируй\", \"нарисуй\", \"создай\"\n\n" +
                  "**Примеры запросов:**\n" +
                  "• \"Сгенерируй космонавта в космосе\"\n" +
                  "• \"Нарисуй волшебный лес с единорогом\"\n" +
                  "• \"Создай футуристический город\"\n\n" +
                  "Просто напиши, что хочешь увидеть! ✨"
        });
    }

    async onMessage(message, context) {
        const text = message.text.toLowerCase().trim();

        // Приветствие
        if (this.greetings.some(greet => text.includes(greet))) {
            await context.reply({
                text: `👋 Привет, ${context.userName}! Готов создавать красивые изображения! 🎨\n\n` +
                      "Напиши, что хочешь увидеть на картинке, и я её сгенерирую!"
            });
            return;
        }

        // Прощание
        if (this.farewells.some(farewell => text.includes(farewell))) {
            await context.reply({
                text: "👋 Пока! Возвращайся, когда захочешь создать новые изображения! 🎨"
            });
            return;
        }

        // Помощь
        if (text.includes("помощь") || text.includes("что ты умеешь")) {
            await this.showHelp(context);
            return;
        }

        // Генерация изображения
        if (this.requests.some(request => text.includes(request)) || this.shouldGenerateImage(text)) {
            await this.generateImage(text, context);
            return;
        }

        // Ответ по умолчанию
        await context.reply({
            text: "🎨 Хочешь, чтобы я сгенерировал изображение? Просто напиши, что хочешь увидеть!\n\n" +
                  "Например: \"Сгенерируй космический корабль\" или \"Нарисуй закат в горах\""
        });
    }

    shouldGenerateImage(text) {
        // Проверяем, содержит ли запрос слова, связанные с визуальным контентом
        const visualWords = ["фото", "картинк", "изображен", "рисун", "арт", "фотограф", "иллюстрац"];
        return visualWords.some(word => text.includes(word));
    }

    async generateImage(userPrompt, context) {
        try {
            // Показываем сообщение о начале генерации
            await context.reply({
                text: "🔄 Генерирую изображение... Это может занять 10-30 секунд ⏳"
            });

            // Извлекаем промпт из запроса пользователя
            const prompt = this.extractPrompt(userPrompt);
            
            if (!prompt) {
                await context.reply({
                    text: "❌ Пожалуйста, уточни, что именно ты хочешь увидеть на изображении.\n\n" +
                          "Например: \"Сгенерируй космонавта в космосе\" или \"Нарисуй волшебный лес\""
                });
                return;
            }

            // Переводим промпт на английский
            const englishPrompt = this.translateToEnglish(prompt);
            
            // Создаем URL для генерации изображения
            const imageUrl = this.createImageUrl(englishPrompt);

            // Сначала отправляем текстовое сообщение с информацией
            await context.reply({
                text: `✅ Вот твое изображение!\n\n**Запрос:** ${prompt}\n**Английский промпт:** ${englishPrompt}\n**Модель:** Lyriel 1.5`
            });

            // Затем отправляем изображение ОТДЕЛЬНЫМ сообщением
            await context.reply({
                image: imageUrl
            });

            // Предлагаем сгенерировать еще
            await context.reply({
                text: "Хочешь создать еще одно изображение? Просто напиши новый запрос! 🎨"
            });

        } catch (error) {
            console.error("Ошибка генерации изображения:", error);
            await context.reply({
                text: "❌ Произошла ошибка при генерации изображения. Попробуй еще раз или измени запрос.\n\nОшибка: " + error.message
            });
        }
    }

    extractPrompt(userText) {
        // Удаляем команды и оставляем только описание
        let prompt = userText.toLowerCase();
        
        // Удаляем команды генерации
        this.requests.forEach(request => {
            prompt = prompt.replace(request, '');
        });
        
        // Удаляем лишние слова
        const removeWords = ["пожалуйста", "мне", "хочу", "можно", "сделай", "создай", "нарисуй", "покажи"];
        removeWords.forEach(word => {
            prompt = prompt.replace(new RegExp(`\\b${word}\\b`, 'g'), '');
        });

        // Очищаем пробелы
        prompt = prompt.trim().replace(/\s+/g, ' ');

        return prompt || userText;
    }

    translateToEnglish(russianPrompt) {
        let englishPrompt = russianPrompt.toLowerCase();
        
        // Заменяем русские слова на английские
        for (const [ru, en] of Object.entries(this.translations)) {
            const regex = new RegExp(`\\b${ru}\\b`, 'gi');
            englishPrompt = englishPrompt.replace(regex, en);
        }

        // Улучшаем промпт для лучшего качества изображения
        const enhancements = [
            "high quality", "detailed", "beautiful", "artistic", 
            "professional", "vibrant colors", "sharp focus",
            "4k", "ultra detailed", "masterpiece"
        ];

        // Добавляем 2-3 случайных улучшения
        const randomEnhancements = enhancements
            .sort(() => Math.random() - 0.5)
            .slice(0, 3)
            .join(', ');

        return `${englishPrompt}, ${randomEnhancements}, digital art`;
    }

    createImageUrl(prompt) {
        // Кодируем промпт для URL
        const encodedPrompt = encodeURIComponent(prompt);
        
        // Создаем URL для API генерации изображений
        return `https://image.pollinations.ai/prompt/${encodedPrompt}?height=1000&width=1000&enhance=true&nologo=true&model=lyriel-1.5-clean`;
    }

    async showHelp(context) {
        await context.reply({
            text: "🎨 **Помощь по боту-генератору**\n\n" +
                  "**Что я умею:**\n" +
                  "• Создавать уникальные изображения по описанию\n" +
                  "• Генерировать арты в разных стилях\n" +
                  "• Работать с русскими и английскими запросами\n\n" +
                  "**Как использовать:**\n" +
                  "Просто напиши, что хочешь увидеть!\n\n" +
                  "**Примеры запросов:**\n" +
                  "• \"Сгенерируй космонавта в космосе\"\n" +
                  "• \"Нарисуй волшебный лес с единорогом\"\n" +
                  "• \"Создай футуристический город будущего\"\n" +
                  "• \"Фото заката на море\"\n" +
                  "• \"Красивый пейзаж с горами\"\n\n" +
                  "**Поддерживаемые темы:**\n" +
                  "Природа, животные, космос, фэнтези, архитектура, портреты и многое другое!\n\n" +
                  "Просто напиши свой запрос и наслаждайся результатом! ✨"
        });
    }
}

// Версия с проверкой доступности изображения
class ImageGeneratorBotWithCheck extends ImageGeneratorBot {
    constructor() {
        super();
    }

    async generateImage(userPrompt, context) {
        try {
            // Показываем сообщение о начале генерации
            await context.reply({
                text: "🔄 Генерирую изображение... Это может занять 10-30 секунд ⏳"
            });

            const prompt = this.extractPrompt(userPrompt);
            
            if (!prompt) {
                await context.reply({
                    text: "❌ Пожалуйста, уточни, что именно ты хочешь увидеть на изображении."
                });
                return;
            }

            const englishPrompt = this.translateToEnglish(prompt);
            const imageUrl = this.createImageUrl(englishPrompt);

            // Проверяем доступность изображения
            const isImageAvailable = await this.checkImageAvailability(imageUrl);
            
            if (!isImageAvailable) {
                await context.reply({
                    text: "❌ Сервис генерации изображений временно недоступен. Попробуй позже."
                });
                return;
            }

            // Сначала отправляем информацию
            await context.reply({
                text: `✅ Вот твое изображение!\n\n**Запрос:** ${prompt}\n**Модель:** Lyriel 1.5`
            });

            // Затем отправляем изображение ОТДЕЛЬНЫМ сообщением
            await context.reply({
                image: imageUrl
            });

            // Предлагаем сгенерировать еще
            await context.reply({
                text: "✨ Хочешь создать еще одно изображение? Просто напиши новый запрос! 🎨"
            });

        } catch (error) {
            console.error("Ошибка генерации изображения:", error);
            await context.reply({
                text: "❌ Произошла ошибка. Попробуй другой запрос или подожди немного."
            });
        }
    }

    async checkImageAvailability(url) {
        try {
            const response = await fetch(url, { method: 'HEAD' });
            return response.ok;
        } catch (error) {
            return false;
        }
    }
}

// Экспорт ботов
if (typeof module !== 'undefined') {
    module.exports = { 
        ImageGeneratorBot, 
        ImageGeneratorBotWithCheck 
    };
}
